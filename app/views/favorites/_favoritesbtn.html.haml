- if post.favorites_by(current_user)
  = link_to post_favorites_path(post.id), method: :delete, class: "favorite" do
    %i.fas.fa-heart#favoritesp
- else
  = link_to post_favorites_path(post.id), method: :post, class: "favorite" do
    %i.far.fa-heart
